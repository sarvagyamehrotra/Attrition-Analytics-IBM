create virtual environment - 
python -m venv app_test
---------------------------------------------------------
activate virtual environment - 
(command: app_test/Scripts/activate) for windows users.
(command: source venv/bin/activate) for mac users.
---------------------------------------------------------
Clone repository-
git clone https://github.com/sarvagyamehrotra/Attrition-Analytics-IBM.git
---------------------------------------------------------
install Dependencies-
pip install -r requirements.txt

this usually takes 3-4 minutes. Let it install completely.
---------------------------------------------------------
Set OpenAI API KEY-
$env:OPENAI_API_KEY = "your_api_key_here" 
---------------------------------------------------------
Run app - 
python app.py
---------------------------------------------------------
Once the server is running, to stop it go back to the powershell terminal and press ctrl+c
---------------------------------------------------------
